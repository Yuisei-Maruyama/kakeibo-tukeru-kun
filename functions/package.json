{
  "name": "kakeibo-line-bot",
  "version": "1.0.0",
  "description": "LINE x Google Calendar Expense Tracker Bot",
  "main": "dist/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && npx @google-cloud/functions-framework --target=webhook --source=dist",
    "lint": "eslint src --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [
    "line",
    "bot",
    "expense-tracker",
    "google-calendar"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/firestore": "^7.1.0",
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/secret-manager": "^5.0.1",
    "@google/genai": "^1.31.0",
    "@line/bot-sdk": "^9.0.0",
    "googleapis": "^128.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
